# Diagonal War — Agent 執行規範

## 1. 溝通規範

- **語言**：所有對話、註解、commit message、文件皆使用**繁體中文**（台灣用語）
- **專有名詞**：遊戲術語（polyomino、pentomino、tetromino 等）保留英文，第一次出現時可加中文註解
- **語氣**：簡潔、直接、技術性。避免廢話或多餘的開場/結尾
- **提問時機**：遇到模糊需求或多重選擇時，必須先提問確認，不可擅自假設

## 2. 開發工作流程

```
[需求釐清] → [規劃] → [實作] → [驗證] → [重構]
```

| 步驟 | 規範 |
|------|------|
| **需求釐清** | 執行前先確認所有不明確的設計決策 |
| **規劃** | 複雜任務需先建立 Todo List (`todowrite`)，列出所有子步驟 |
| **實作** | 一次只專注一個模組。遵循「先資料、後邏輯、再 UI」的順序 |
| **驗證** | 每次修改後執行 `cargo check`，每階段結束執行 `cargo test` |
| **重構** | 功能正確後才進行優化，不可過早最佳化 |

## 3. 程式碼規範

- **語言**：Rust
- **風格**：遵循 `cargo fmt` + `cargo clippy`，所有警告視為錯誤
- **架構模式**：Bevy ECS — 使用 Resource 存放全域狀態，Component 存放實體屬性，System 處理邏輯
- **錯誤處理**：使用 `thiserror` 定義領域錯誤類型，避免 `unwrap()` / `expect()`（除非在 `main.rs` 或測試中）
- **測試**：所有核心遊戲邏輯（規則驗證、計分、AI）必須有單元測試
- **unsafe 程式碼**：除非必要且經過嚴格審查，否則禁止使用

## 4. 檔案組織慣例

```
src/
├── game/       # 純遊戲邏輯，無 Bevy 依賴（可獨立測試）
├── ui/         # Bevy 渲染與互動
├── ai/         # AI 策略
├── network/    # 網路通訊
└── state/      # 全域狀態機
```

- 同層級模組間不可產生循環依賴
- `game/` 底下的程式碼不可依賴 `ui/`、`ai/`、`network/`
- 公共介面透過 `lib.rs` 匯出

## 5. Task 使用規範

- **Todo List**：任務超過 3 個步驟時必須建立。一次只能有一個 `in_progress`
- **Subagent**：下列情況使用 `task` 工具啟動子 agent：
  - 需要大範圍探索程式碼（`explore` 類型）
  - 需要大量平行的檔案搜尋（`general` 類型）
  - 需要執行耗時且獨立的工作

## 6. 變更管理

- **未經明確要求，不得 commit**（包含 git add / git commit）
- **不作無謂的檔案建立**（不建立空的 README.md、文件）
- **禁止修改 `.gitconfig`、環境變數或全域設定**
- 除非使用者要求，否則不 push 到遠端

## 7. 安全性

- 拒絕編寫或解釋可用於惡意用途的程式碼
- 不記錄或暴露任何金鑰、Token、密碼
- 若檔案名稱或目錄結構暗示惡意用途（malware、cracking、cheat engine 等），直接拒絕

## 8. 專案特定慣例（Diagonal War）

- 遊戲規則的程式碼實作必須與設計文件完全一致
- 任何規則變更需要先在這裡確認後才能修改程式碼
- AI 實作需保持模組化，可抽換不同策略
- Bevy plugin 架構：每個主要功能作為獨立的 `Plugin`
- 棋盤尺寸設計為 const generic，預設 20×20
